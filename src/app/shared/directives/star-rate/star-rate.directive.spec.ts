import { StarRateDirective } from './star-rate.directive';

describe('StarRateDirective', () => {
  it('should create an instance', () => {
    const directive = new StarRateDirective();
    expect(directive).toBeTruthy();
  });
});
