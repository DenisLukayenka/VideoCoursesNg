import { DateRelevantDirective } from './date-relevant.directive';

describe('DateRelevantDirective', () => {
  it('should create an instance', () => {
    const directive = new DateRelevantDirective();
    expect(directive).toBeTruthy();
  });
});
